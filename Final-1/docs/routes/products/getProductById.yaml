
  /api/producto:
    get:
      summary: Muestra el producto buscado por id
      tags:
        - Productos
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/requestBodies/FindProduct'
        
      responses: 
        "201":
          descriptions: Producto por id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Product'